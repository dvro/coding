#include <stdio.h>
#include <stdlib.h>
#include <array_utils.h>

int main(void)
{
/*
 *
	int *array = (int *)calloc(9, sizeof(int));
	int i;
	for (i = 0; i < 9; i++)
		array[i] = i % 2 + 1;

	int len = erase_all_x(&array, 9, 1);
	printf("len = %d (4)\n", len);

	for (i = 0; i < len; i++)
		printf("array[%d] == %d\n", i, array[i]);
	free(array);
*/

	return 0;
}


